// Cart is now managed via CartContext (lib/context/CartContext.tsx) backed by the DB.
// This file is kept to avoid broken imports during migration — delete it once all references are gone.
export {}
